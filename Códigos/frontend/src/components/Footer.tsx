// import { useNavigate } from "react-router-dom";

const Footer = () => {
    // let navigate = useNavigate();

    return (
        <div className="App">
            <footer>
                Já já faço
            </footer>
        </div>
    );
};

export default Footer;
